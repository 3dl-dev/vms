# tests/qemu/facility_defects_floor.txt
#
# DERIVED FLOOR (vms-d894) on the number of entries in facility_defects.sh's
# DEFECTS list. Read by cmd_coverage's section 6 -- see that function and the
# file header's "vms-d894" paragraph for what this does and does not prove.
#
# This is a SEPARATE artifact on purpose. facility_defects.sh's own anchor
# check (section 4, vms-279) already requires a defect deletion to touch two
# files (facility_defects.sh + a suite source); this floor adds a THIRD, so
# a shrink below the number below is no longer confined to the deleter's own
# edit -- it has to also lower this file, visibly, in a diff with no other
# content.
#
# NOT TAMPER-PROOF: lowering the number below to match a deletion still
# passes the check. Lower it ONLY for a tracked, intentional removal of a
# defect, in the same change that removes it and explains why -- never to
# make an unrelated deletion pass quietly.
#
# THIS IS NO LONGER THE ONLY COUNT FLOOR, and it is the weaker one (vms-d894
# round 2). Section 6b of cmd_coverage derives a second floor from
# tests/qemu/facility_negctl_observed.tsv -- the number of defects a PAST RUN
# OF THE DRIVER ACTUALLY EXECUTED, which no edit to this file can raise or
# lower. Both floors are applied and neither replaces the other, so a record
# covering fewer defects can never lower the number below.
#
# To raise the floor after legitimately ADDING defects, rerun and paste the
# result below the comments:
#   awk '/^DEFECTS="/{f=1;next} f&&/^"/{exit} f' tests/qemu/facility_defects.sh | wc -l
#
# Value as of the commit that added this file (vms-d894): 42 entries.
# Raised to 43 by vms-2b2's register-adopt-pid-not-reported (see its own
# entry in facility_defects.sh for what it covers and why).
# Raised to 46 by vms-053's lock-enq-immediate-grant-status-wrong,
# lock-deq-status-wrong and lock-convert-mode-not-updated (the first
# mutations anchored inside vms_ioctl_enq/deq/convert's own bodies -- see
# those entries in facility_defects.sh for what each covers and why).
# Raised to 48 by vms-68e's proctab-getjpi-nonexpr-status-wrong and
# proctab-procscan-nonexpr-status-wrong (the first mutations anchored
# inside vms_ioctl_getjpi/procscan's own bodies -- see those entries in
# facility_defects.sh for what each covers and why).
# Raised to 51 by vms-2e7's devtab-dassgn-status-wrong,
# devtab-getdvi-devnam-status-wrong and devtab-devscan-found-status-wrong
# (the first mutations anchored inside vms_ioctl_dassgn/getdvi/devscan's
# own bodies -- see those entries in facility_defects.sh for what each
# covers and why).
# Raised to 55 by vms-177's eflag-setef-status-inverted,
# eflag-readef-status-inverted, eflag-dacefc-status-wrong and
# eflag-dlcefc-status-wrong (the first mutations anchored inside
# vms_ioctl_setef/readef/dacefc/dlcefc's own bodies -- see those entries in
# facility_defects.sh for what each covers and why). A fifth,
# eflag-ascefc-reassoc-status-wrong, was written and then DROPPED: real
# execution showed vms_ioctl_ascefc's own re-association status is only
# reachable in this tree through an unsynchronized race between two
# processes in test_kmod_eflag_mproc.c (parent and child both call
# sys$ascefc on the same cluster name immediately after fork(), no
# ordering between them), so which of two possible assertions goes red is
# genuinely nondeterministic -- structurally incompatible with this
# manifest's exact-match model. Left for a future item that either adds
# synchronization to that test or finds a differently-scoped target.
# Raised to 57 by vms-2ed's eflag-wflor-status-wrong and
# eflag-wfland-status-wrong (the first mutations anchored inside
# vms_ioctl_wflor/wfland's own bodies -- see those entries in
# facility_defects.sh for what each covers and why).
# Raised to 58 by vms-400's eflag-ascefc-reassoc-status-wrong -- the fifth
# attempt named above at line 51, this time landing: test_kmod_eflag_mproc.c
# was fixed to de-race its parent/child $ASCEFC calls (a token handshake so
# the parent always creates the cluster and the child always re-associates)
# before this entry was written, so the assertion it names is no longer
# scheduler-dependent. See its own entry in facility_defects.sh for the
# full account.
# Raised to 60 by vms-221's rms-create-filespec-not-translated -- the first
# entry to target vmsrms/rms_core.c (a userspace consumer of vmsfs's
# translation, same class as the vmsdcl entries) instead of src/kernel or
# src/vmsdcl, anchoring the new test_syssvc_rms_scratch_create suite so it
# is not left with a permission-only claim of being reddenable. cmd_selftest
# also grew a copy of src/vmsrms into its throwaway tree for this entry (see
# facility_defects.sh's cmd_selftest, "libvms, vmsdcl and vmsrms are copied
# too"). (The 59th, following #176's addition to the manifest, was already
# on the branch this rebased onto.)
# Raised to 61 by vms-e5c's scratch-dir-owner-not-system -- anchors
# test_syssvc_scratch_writable, whose own duplicated directory-provisioning
# helper (not src/ovmx_init/ovmx_init.c, which this suite never links) is
# the mutation target; see its own entry in facility_defects.sh for what it
# covers and why.
# Raised to 62 by vms-d37's lnm-delete-noop -- the first mutation targeting
# kernel-core/vms_lnm.c (executive-resident LNM$SYSTEM), anchoring the new
# test_syssvc_lnm_crossproc suite's cross-process DEASSIGN/SYSTEM assertion.
# See its own entry in facility_defects.sh for what it covers and why.
# Raised to 63 by vms-4d7's register-continue-identity-dropped -- anchors the
# new test_syssvc_identcont suite (image activation continues its activator's
# VMS identity, Option B), the first mutation targeting
# vms_proc_continue_identity() in kernel/vms_module.c. See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 64 by vms-48ab's lnm-manager-delete-noop -- anchors the new
# test_syssvc_lnm_system suite (the vmslnm-MANAGER API's LNM$SYSTEM path,
# as distinct from the sys$ API test_syssvc_lnm_crossproc.c already covers),
# the first mutation targeting vmslnm/lnm_client.c. See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 65 by vms-aba's lnm-group-scope-collapsed -- anchors the new
# test_syssvc_lnm_groupjob suite (LNM$GROUP/LNM$JOB executive residency, the
# deferred other half of vms-d37's LNM$SYSTEM work), the first mutation
# targeting derive_scope_key()'s LNM$GROUP case in kernel-core/vms_lnm.c. See its
# own entry in facility_defects.sh for what it covers and why.
#
# NOTE: this file was not updated when lnm-privilege-check-bypassed and
# mbx-not-shared landed (DEFECTS reached 67 without a matching entry here).
# Not fixed retroactively by this edit -- the floor is a MINIMUM, and 67 >=
# 65 still passed -- but flagged so the gap is not mistaken for a re-derived
# count. The line below only asserts the count as of THIS entry, vms-5bdc.
#
# Raised to 68 by vms-5bdc's p0-map-not-recorded -- vms-68f.i, the P0
# program-region bookkeeping foundation of the Option A in-process image
# activation design (docs/design-in-process-activation.md Part II). The
# first mutation targeting the new kernel/vms_p0.c, anchoring the new
# test_kmod_p0 suite (VMS_IOCTL_P0_MAP/P0_UNMAP, reflected through
# $GETJPI's new p0_base/p0_limit fields). See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 70 by vms-6ba's p1-map-not-recorded and p0-unmap-clears-p1 --
# vms-68f.ii, P1 control-region persistence and the P0/P1 distinction
# (same design). p1-map-not-recorded is the first mutation targeting the
# new kernel/vms_p1.c (VMS_IOCTL_P1_MAP, mirroring p0-map-not-recorded's
# shape on the new file); p0-unmap-clears-p1 targets vms_ioctl_p0_unmap()'s
# own body (kernel/vms_p0.c) and anchors the new test_kmod_p1 suite's key
# property, "P0 deleted on rundown, P1 survives", reflected through
# $GETJPI's new p1_base/p1_limit fields. See their own entries in
# facility_defects.sh for what each covers and why.
# Raised to 71 by vms-6f1's imgact-p1-not-protected -- vms-68f.iv, in-process
# image activation (imgact_activate(), the fork-per-image replacement). The
# first mutation targeting libvms/syssvc/sys_imgact.c, anchoring the new
# test_syssvc_imgact_inproc suite's enforced-half property, "a User-mode
# in-process image cannot corrupt DCL's critical P1" (the p1_protect the
# activation applies before ENTER_IMAGE). See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 74 by vms-cd48's image-rundown-leaks-user-lock -- vms-68f.v,
# SYS$RUNDWN image-scoped resource release (image rundown dequeues the locks,
# deassigns the channels and flushes the ASTs an activated image held at USER
# mode, while process-permanent inner-mode state survives). Targets
# vms_ioctl_image_rundown()'s own body (kernel/vms_access.c) and anchors the
# new test_kmod_rundown suite's key property. (This also closes the standing
# gap the note above flags: super-mode-escalation and image-rundown-without-
# entry, vms-68f.iii, had pushed the true count to 73 without the floor being
# raised; 74 is the re-derived `echo $DEFECTS | wc -w`.) See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 75 by vms-db2's consumer-import-not-bound-to-resident -- the
# §A.8-remainder import-binding-to-resident-shareable sub-step: a RUN'd image's
# .vms$imp imports must bind to the ALREADY-RESIDENT LIBVMS$SHR/DECC$SHR the
# process holds, not a private copy (which would share nothing). Targets
# libvms/syssvc/imgact_prodreg.c (imgact_bind_imports_resident's GOT-cell store)
# and anchors the new test_imgact_bind suite's genuine-sharing property,
# proven in isolation without /dev/vms. See its own entry in facility_defects.sh.
# (test_imgact_bind was test_syssvc_imgact_bind through #225 -- renamed by a
# main-red classification fix: it never touches /dev/vms and the
# test_syssvc_ prefix wrongly put it under ci.yml's kernel-executive-negative-
# control "must honest-skip 77" contract.)
# Raised to 76 by vms-db2's publish-does-not-populate-registry -- the
# §A.8-remainder gap 1 (publish the registry at runtime): IMGACT.EXE hands the
# resident LIBVMS$SHR/DECC$SHR producer bases it mapped across to the registry
# (imgact_publish_producers()) so a later in-process RUN's .vms$imp imports can
# resolve against them -- the binding mechanism (consumer-import-not-bound-to-
# resident) had nothing to bind against until this. Targets
# libvms/syssvc/imgact_prodreg.c (imgact_publish_producers's register loop) and
# anchors the new test_imgact_publish suite's publish-then-share property,
# (landed directly as test_imgact_publish, never as test_syssvc_imgact_publish
# -- see test_imgact_bind's note above; same classification reasoning.)
# proven in isolation without /dev/vms. No image class is flipped -- real images
# still fork (the IMGACT-side glue's end-to-end proof rides on the native-link
# runtime, vms-0b8). See its own entry in facility_defects.sh.
# Raised to 77 by vms-db2's realimg-auxv-argc-wrong -- the §A.8-remainder gap 2
# + the FLIP: a REAL image (entered through the SysV auxv _start ABI, its
# .vms$imp imports bound to the resident producer, ending with the resident
# SYS$EXIT that returns to DCL) now runs IN-PROCESS, same PID, no fork. Targets
# libvms/syssvc/sys_imgact.c (imgact_build_auxv_stack's argc word) and anchors
# the new test_syssvc_imgact_realimg suite -- proven against a real /dev/vms
# (same VMS+Linux PID, SYS$EXIT returns, an event flag the image set flows back,
# resident TLS shared). See its own entry in facility_defects.sh.
# Raised to 78 by vms-db2's extern-interp-check-rejects-ours -- the
# §A.8-remainder item 2 / EXTERNAL-image flip: a genuinely external LINK.EXE
# image names its loader (IMGACT.EXE) in PT_INTERP, and in-process activation IS
# that loader, so imgact_activate now accepts such a PT_INTERP and runs the image
# IN DCL's process (a foreign interp still forks). Targets libvms/syssvc/
# sys_imgact.c (imgact_interp_is_ours's basename match) and anchors the new
# test_syssvc_imgact_extern suite -- proven against a real /dev/vms (same
# VMS+Linux PID, SYS$EXIT returns, event flag flows back, resident TLS shared),
# with an in-suite foreign-interp control proving a foreign loader still forks.
# Raised to 79 by vms-db2's nonres-producer-not-mapped -- the §A.8-remainder
# item 1 NON-RESIDENT-producer sub-step: a real image may import a universal
# from a shareable DCL does NOT hold resident; imgact_map_producer maps it into
# DCL's process (once, process-permanent -- re-homed from IMGACT.EXE's
# load_ovmx_producer), registers it, and binds the consumer to that one mapped
# instance. Targets libvms/syssvc/sys_imgact.c (imgact_map_producer's registry
# store) and anchors the new test_syssvc_imgact_nonres suite -- proven against a
# real /dev/vms (same VMS+Linux PID, SYS$EXIT returns, and a SHARED-instance
# proof: the suite mutates the mapped producer's counter to 100 and a second run
# observes 101, not a private-copy 1). Fenced to a pure leaf producer (no own
# PT_TLS, no C-RTL bootstrap that would reprogram the thread pointer -- the
# deferred remainder, which forks). See its own entry in facility_defects.sh.
# Raised to 80 by vms-db2's tlsdesc-offset-not-biased -- the §A.8-remainder item 4
# OWN-PT_TLS sub-step (the DTV-append / TLS-absorb case #236 fenced): an image with
# its OWN PT_TLS now runs in-process, given its own TLS block whose .vms$tls TLSDESC
# entries imgact_setup_own_tls biases against DCL's UNCHANGED thread pointer, so the
# image's __thread data is its own while DCL's TP/TLS and the resident universals'
# TLS are untouched (the no-clobber crux). Targets libvms/syssvc/sys_imgact.c
# (imgact_setup_own_tls's TP-relative bias) and anchors the new test_syssvc_imgact_tls
# suite -- proven against a real /dev/vms (same VMS+Linux PID, SYS$EXIT returns, own
# TLS distinct + correct, DCL's TP and a DCL __thread datum unchanged, resident TLS
# still reached, event flag flows back). See its own entry in facility_defects.sh.
# Raised to 81 by vms-as1's ast-deliver-crossmode-escalation -- the AST-delivery
# access-mode SECURITY fix: vms_ioctl_deliverast (kernel/vms_ast.c) is now bounded
# below by the caller's current access mode, so a user-mode $SETAST(1) can no
# longer drain and execute the KERNEL/EXEC/SUPER AST queues. The mutation reverts
# the scan lower bound to PSL_C_KERNEL and anchors the new test_syssvc_ast_secmode
# suite's cross-mode assertion. See its own entry in facility_defects.sh.
# Raised to 82 by vms-pv1's setprv-grants-unauthorized -- the $SETPRV MUTATION
# write-through: sys$setprv (src/libvms/syssvc/sys_misc.c) now routes through the
# executive (vms_kif_setprv -> vms_ioctl_setprv, kernel/vms_access.c), which
# authorizes the grant against the caller's AUTHORIZED mask instead of a process
# writing pcb->cur_privs for itself. The mutation drops the authorized-subset
# intersection so an unprivileged caller is granted any privilege it names, and
# anchors the new test_syssvc_setprv suite's denial-path assertion (plus the two
# raw-ioctl setprv assertions test_kmod_access already carries). See its own
# entry in facility_defects.sh.
# Raised to 83 by vms-vx1's devtab-getdvi-userspace-alloc-dropped -- the
# executive-R3 veracity gate (epic vms-6b8). The FIRST device-table entry whose
# target is the USERSPACE C-API layer (libvms/syssvc/sys_device.c) rather than
# the executive: every devtab entry above mutates kernel/vms_devtab.c, so its
# gating check-4 red is satisfied by the raw-ioctl test_kmod_devtab regardless
# of what the sys$getdvi translation does -- a C-API reader that silently
# reverted to a per-process device view (the vms-dv1 fake class) would pass them
# all. This entry mutates sys_device.c's own DVI$_DEVCHAR handler (drops the
# DEV$M_ALL bit it OR's in from the executive's info->allocated) and scopes
# suites_red to test_syssvc_getdvi ALONE -- the only suite that reaches the
# executive's shared allocation through the shipped public sys$getdvi -- so the
# control fires IFF the C API stops reflecting executive-owned state, making
# INV-6 ("cannot silently revert to a per-process fake") a first-class firing
# negctl at the C-API layer. See its own entry in facility_defects.sh.
# Raised to 84 by vms-95a's dclast-super-mode-escalation -- the $DCLAST
# access-mode SECURITY fix, the companion to vms-68f.iii's setmode SUPER gate
# (super-mode-escalation) and vms-as1's AST-delivery bound (ast-deliver-
# crossmode-escalation): vms_ioctl_dclast (kernel/vms_ast.c) gated declaring an
# AST at KERNEL and EXEC but let SUPER fall through unchecked, so a USER-mode
# caller could plant a SUPER AST without CMEXEC/CMKRNL (surviving image rundown,
# which flushes only USER-mode ASTs, to run when DCL next drains SUPER). The
# mutation disables the added SUPER gate (0 &&) and anchors the new SUPER-refusal
# assertion added to test_syssvc_ast_secmode's PART B (the credential-dropped
# USER-mode child). See its own entry in facility_defects.sh.
# Raised to 85 by vms-3e8's disk-backing-not-resolved -- the executive now
# enumerates the node's virtio disks into DK units (DKA0:/DKA100:/...) at
# module init and resolves a unit to its backing Linux block device
# (VMS_IOCTL_DISK_RESOLVE, kernel/vms_devtab.c). The mutation zeroes the one
# backing-name copy in vms_ioctl_disk_resolve() -- success reported, the name
# not handed back -- and anchors the new test_kmod_disk suite's two
# backing-name assertions. See its own entry in facility_defects.sh for what
# it covers and why.
# Raised to 86 by vms-6bdf's resdir-master-csid-not-reported -- the missing
# per-facility negative control for test_kmod_resdir (vms-ci.5 DB / #264),
# flagged by the vms-3e8 agent as a coverage-gate gap: that suite had NO
# negctl entry at all. Targets vms_ioctl_get_resmaster()'s (kernel/vms_lock.c)
# sole master_csid copy-out; the internal res->master_csid state the
# mastering decision and is_local_master are computed from is untouched, so
# this isolates the DIAGNOSTIC READBACK from the ACTUAL mastering state. See
# its own entry in facility_defects.sh for what it covers and why.
#
# Raised to 89 by vms-6d6's lnm-searchlist-equiv-truncated,
# rightslist-general-hex-as-decimal and sysuaf-uic-radix-decimal -- the last
# three pre-existing coverage-gate gaps from #284/#272 (vms-420, vms-d37):
# test_syssvc_lnm_searchlist, test_syssvc_rightslist and
# test_syssvc_sysuaf_uic_base had NO per-facility negctl entry at all, the
# same class of hole vms-6bdf/#293 closed for test_kmod_resdir. Targets
# kernel-core/vms_lnm.c (the multi-value DEFINE's stored equivalence count),
# libvms/rtl/rightslist.c (RIGHTSLIST.DAT's %X general-identifier radix) and
# libvms/include/sysuaf.h (SYSUAF.DAT's shared UIC read radix) respectively.
# Every in-scope suite is now covered -- `coverage` names none as ungated.
# All three were mutation-verified against a real /dev/vms via
# run_facility_negctl.sh: each reddens the harness, vms.ko stays loaded, and
# the facility's own suite(s) go red with nothing outside the declared set
# failing. See their own entries in facility_defects.sh for what each covers
# and why, and for the genuine knock-on suites each mutation was measured to
# also reach (test_syssvc_ident for two of the three; test_syssvc_setuai for
# sysuaf-uic-radix-decimal, found by running it, not predicted).
#
# Raised 89 -> 90 (vms-a7e): added proc-acct-not-sourced, the negative control
# for the executive per-process accounting facility (fill_proc_acct in
# kernel/vms_proctab.c). Mutation-verified via inject_and_run.sh -- removing
# both fill_proc_acct() call sites reddens test_kmod_procacct, and the two
# knock-on suites test_syssvc_showproc and test_syssvc_procnam, with nothing
# outside the declared set failing.
#
# Raised 90 -> 91 (vms-05b): added delprc-privcheck-bypassed, the negative
# control that anchors test_syssvc_delprc -- the suite #318 (vms-1a8) added
# without a matching negctl defect, leaving it in-scope but unanchored and
# reddening the coverage meta-check on main. PRODUCT-half mutation in
# libvms/syssvc/sys_process.c: making sys$delprc's `if (!authorized)` refusal
# unreachable lets a caller without GROUP or WORLD delete a same-group target,
# reddening exactly the P3 SS$_NOPRIV assertion (the "still ALIVE" sibling
# stays green -- the killed target is the suite's own $CREPRC child, so it
# becomes a zombie and kill(pid, 0) still returns 0; see the entry in
# facility_defects.sh). Mutation-verified against a real /dev/vms via
# run_facility_negctl.sh: the harness reds, vms.ko stays loaded, and
# test_syssvc_delprc goes red with nothing outside the declared set failing.
#
# Raised 91 -> 92 (vms-9e2): added getjpi-pidarg-discarded, the negative
# control that anchors test_syssvc_getjpi_pidarg -- the suite this change
# adds for "F$GETJPI honors its pid argument", in-scope but otherwise
# unanchored (the main-red hole delprc-privcheck-bypassed just closed for
# test_syssvc_delprc). PRODUCT-half mutation in vmsdcl/dcl_lexical.c:
# forcing lex_getjpi's target resolution always-true down the self branch
# (`if (1 ...)`) discards the pid argument, so F$GETJPI answers about the
# caller again (the vms-9e2 facade), reddening the four pid-target identity
# assertions in the suite's executive scenario (PRCNAM/USERNAME/PID and the
# paired "did NOT report the caller's identity"); the self-form and liveness
# assertions stay green. Mutation-verified against a real /dev/vms via
# run_facility_negctl.sh.
#
# Raised 92 -> 93 (vms-9003): added mbx-wrtattn-not-fired, the negative control
# that anchors test_syssvc_mbx_wrtattn -- the new suite for mailbox
# WRITE-ATTENTION ASTs (IO$_SETMODE|IO$M_WRTATTN), the long-pole executive
# facility MMK's send_cmd_and_wait waits on (spine #4). PRODUCT-half mutation in
# kernel-core/vms_mbx.c: forcing the write path's AST allocation to NULL drops
# the cross-process write-attention delivery while the arm, the rendezvous and
# the write all stay green -- reddening exactly the suite's five fire/param/
# one-shot assertions. See its own entry in facility_defects.sh.
#
# Raised 93 -> 94 (vms-feb): added hiber-ast-not-delivered, the negative control
# that anchors test_syssvc_hiber_ast -- the new suite for ASYNCHRONOUS AST
# delivery INTERRUPTING $HIBER (VMS_IOCTL_HIBER/WAKE + vms_ast_notify_arrival),
# the $HIBER/$WAKE + write-attention-AST rendezvous MMK's send_cmd_and_wait
# needs (spine #4, vms-b23). PRODUCT-half mutation in kernel-core/vms_ast.c:
# removing vms_ast_notify_arrival()'s exec_cv_broadcast(&proc->hiber_wq) leaves a
# cross-process write-attention AST queued but never wakes the hibernating
# reader to run it, so its sys$hiber() deadlocks -- reddening exactly the suite's
# "$HIBER was interrupted / AST ran and returned" assertion and its paired
# astprm round-trip check, while the self-wake path (vms_ioctl_wake's own
# broadcast) and test_syssvc_mbx_wrtattn (explicit $SETAST drain, never hibers)
# stay green. See its own entry in facility_defects.sh.
# Raised 94 -> 95 by vms-786's dcl-sysinput-mbx-not-read -- the negative control
# that anchors the new test_syssvc_mbx_dcldrv suite (the shipped DCL.EXE reads
# SYS$INPUT from / writes SYS$OUTPUT to a mailbox, the persistent-DCL-over-
# mailboxes drive MMK's send_cmd_and_wait needs, spine #4). PRODUCT-half
# mutation in src/vmsdcl/dcl_mbx.c: forcing the reader thread's per-message
# forwarded length to zero (mlen = 0) drains the SYS$INPUT mailbox but delivers
# nothing to DCL's command loop, so the driven DCL receives no command and
# delivers no result -- reddening exactly the suite's one driven-result
# assertion while the parent's own $CREMBX and command writes stay green. See
# its own entry in facility_defects.sh for what it covers and why.
# Raised 95 -> 96 by vms-b23's mmk-drive-command-not-sent -- the negative control
# that anchors the new test_syssvc_mmk_drive suite (the shipped MMK.EXE drives a
# real build through its persistent mailbox-driven DCL subprocess, self-host
# spine #4). PRODUCT-half mutation in tests/corpus/tier3-mmk/ovmx/ovmx_mmk_sp.c:
# forcing sp_send's per-record length to zero delivers empty command records into
# the SYS$INPUT mailbox, so the driven DCL receives neither the action command
# nor the end-of-command marker -- reddening the suite's driven-result assertion
# and (same root) its completion assertion, while a no-executive run still
# honest-skips. See its own entry in facility_defects.sh for what it covers and why.
96
