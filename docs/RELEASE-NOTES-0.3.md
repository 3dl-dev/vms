# OVMX V0.3 Release Notes

Generated by `tools/gen_release_notes.py` from the merged history since `0.2` (100 commits). Do not hand-edit this file -- re-run the tool against the cut commit instead.

## Features

- release: bump OVMX_PRODUCT_VERSION V0.2 -> V0.3 for the 0.3 cut
- vms-5bc: LIVE-verify the op-6 credit-first rejoin fix (#291) — spec §4(O.18) (#295)
- vms-6d6: complete the per-facility coverage gate (searchlist/rightslist/sysuaf uic negctls) (#294)
- vms-6bdf: add the missing per-facility negative control for test_kmod_resdir (#293)
- vms-08c: device-corpus programs genuinely run-pass under QEMU's real /dev/vms (#292)
- vms-46f: rejoin — initiate op-6 special-credit request BEFORE op 0x02 (spec §4(O.17)) (#291)
- vms-9af: rejoin keeps the COORDINATOR VC lean -- op 0x02 rides ss=12 not 24 (spec 4(O.16), necessary-not-sufficient) (#289)
- vms-a17e: vms.ko constructs SYSTEM's identity at module init; PID 1 reads SYSUAF zero times (#288)
- vms-21a: STARTUP.COM becomes a real STDRV phased startup driver (#286)
- vmslnm: DEFINE/SHOW LOGICAL carry the full search list, not just index 0 (vms-420) (#284)
- release: generate release notes from git history + tie install/upgrade guides to the e2e gates (vms-55a, epic vms-a84) (#285)
- release: generalized release-acceptance gate boots the CUT artifact, verifies shipped version (vms-a86f) (#283)
- boot: STARTUP.EXE reads OVMXVMSSYS.PAR, sets real hostname from SCSNODE (vms-b6a7) (#282)
- product: kit-format seed-once metadata preserves site config on upgrade (vms-2c9) (#281)
- release: install->UPGRADE->boot e2e gate proves the upgrade-safety gap (vms-f05) (#280)
- release: cut-release.sh — reproducible release bundle from a clean tree (vms-d73) (#279)
- PRODUCT INSTALL: land a real kit on a real vmsfs volume (vms-df9) (#278)
- dcl: MOUNT/DISMOUNT really mount(2)/umount(2) vmsfs volumes (vms-651) (#277)
- sysgen: move the parameter file onto the system disk as SYS$SYSTEM:OVMXVMSSYS.PAR, real vmsfs versions (vms-d34) (#276)
- kit: define the OVMX product kit container + factory packer (vms-0b6) (#275)
- test: gate the retired per-process executive fakes against reintroduction (vms-fk1) (#274)
- executive: enumerate virtio disks into DK units + resolve unit->block device (vms-3e8) (#273)
- init: STRIP ALL install machinery from PID 1 — mount-or-halt bootstrap + STDRV F1 (vms-2f0) (#271)
- lnm/dcl: migrate host LNM-fake tests to executive path + scope SHOW LOGICAL qualifiers (vms-d37) (#272)
- Executive: named mailboxes connect two unrelated processes — $ASSIGN-by-name resolves LNM$SYSTEM to the MBAn: unit (vms-mb1) (#269)
- distro: master a pre-installed distribution disk image and boot it to login (vms-8ab) (#268)
- feat: SHOW LOGICAL lists the executive LNM tables cross-process (vms-96e2) (#266)
- tools: add vmsfs_master — the VMSFS media-mastering build tool (vms-791) (#265)
- Upgrade attestation proofs to Bitcoin-anchored
- docs: §6 RULED — STRIP ALL install machinery from PID 1 (operator, 2026-08-10)
- docs: land the installer + boot design records to main (0.4 spine prereq)
- security: close the $DCLAST SUPER-mode access-mode gap (vms-95a) (#261)
- qa: userspace-layer device-table veracity negctl — sys$getdvi cannot silently revert to a per-process fake (vms-vx1) (#260)
- lckdef.h: correct nine wrong LCK$M_ flag bits against the VAX oracle (vms-982) (#259)
- syssvc: sys$getdvi/sys$device_scan read the executive device table, not a host fake (vms-dv1) (#258)
- libvms: implement 8 lib$/ots$ RTL entry points — corpus run-pass 83 -> 91 (vms-801 R2.2 batch 4) (#256)
- self-host S4: OVMX builds LINK.EXE from within, byte-stable gen2==gen3, no bash (vms-62b) (#255)
- dcl: SET PROCESS/PRIVILEGES HIDE->MATCH via executive-backed sys$setprv (vms-e5d7) (#253)
- self-host S3.2: BUILD.COM — DCL driver compiles+links+runs with ZERO bash (vms-615) (#251)
- executive: $SETPRV mutation routes through /dev/vms — executive owns/authorizes privilege state (vms-pv1, R3) (#250)
- libvms: implement 16 lib$ RTL entry points — corpus run-pass 73 -> 83 (vms-801 R2.2 batch 3) (#249) (#249)
- self-host S3.1: LINK.EXE is now an OVMX image — RMS I/O shim + self-link (vms-b5a) (#247)
- libvms: implement 22 lib$/ots$ RTL entry points — corpus run-pass 59 -> 73 (vms-801 R2.2 batch 2) (#248)
- executive: AST delivery respects the caller's access mode — fix user-mode $SETAST draining kernel-mode AST queues (vms-as1) (#245)
- libvms: implement 21 missing lib$ RTL entry points — corpus run-pass 42 -> 59 (vms-801 R2.2) (#246)
- conformance: commit HONEST corpus baseline + convert CI to a falsifiable run-pass floor (vms-801 R2.0) (#244)
- imgact: FLIP the own-PT_TLS class in-process — DTV-append TLS-absorb over DCL's unchanged TP (vms-db2, §A.8 item 4) (#242)
- imgact: FLIP images whose .vms$imp names a NON-RESIDENT producer in-process — map the missing shareable in DCL's process (vms-db2) (#236)
- imgact: FLIP the PT_INTERP-bearing EXTERNAL image class in-process (vms-db2) (#234)
- imgact: FLIP a real-image class to in-process activation — auxv entry + SYS$EXIT returns to DCL (vms-db2) (#231)
- Upgrade attestation proofs to Bitcoin-anchored
- imgact: bind a RUN'd image's .vms$imp imports to the RESIDENT shareable (vms-db2, in-process activation sub-step) (#225)
- dcl: wire DCL's real P1 control region + prove the same-PID payoff (vms-68f.v, partial) (#224)
- kernel: SYS$RUNDWN releases image-scoped channels/locks/ASTs at image rundown (vms-68f.v) (#223)
- dcl: in-process image activation (imgact_activate) — RUN runs in DCL's process, no fork (vms-68f.iv) (#222)
- kernel: access-mode transition + boundary enforcement (vms-68f.iii) (#220)
- kernel: vms.ko P1 control-region persistence + P0/P1 distinction (vms-68f.ii) (#218)
- kernel: vms.ko P0 program-region map/free (VMS_IOCTL_P0_MAP/P0_UNMAP, vms-68f.i) (#216)
- docs(vms-68f): full Option A design — true in-process image activation (no fork) (#213)
- tear out the tests/integration negctl declaration-gate theatre (vms-b44) (#212)
- Executive: $CREMBX/$DELMBX mailboxes executive-resident, cross-process IPC via /dev/vms (vms-d44) (#210)
- feat: enforce SYSNAM/GRPNAM privilege on LNM$SYSTEM/LNM$GROUP mutation (vms-5b7) (#208)
- ods2: increment 9 (vms-0f3) -- RMS VAR-record framing, full real-VAX read-back (vms-600 acceptance) (#207)
- ods2: increment 8 (vms-0f3) -- fix DIR efblk/hiblk; full clean MOUNT achieved (#206)
- feat: LNM$GROUP and LNM$JOB become executive-resident, like LNM$SYSTEM (vms-aba) (#205)
- ods2: increment 7 (vms-0f3) -- splice-isolate BADIRECTORY to the writer's own [000000] MFD; fix dir_verlimit field-confusion (#204)
- ods2: increment 6 (vms-0f3) -- genuinely fragment INDEXF.SYS, resolve FILENUMCHK; BADIRECTORY is the new wall (#203)
- ods2: increment 5 (vms-0f3) -- SECURITY.SYS format is derivable (A); MOUNT/OVERRIDE is not a fix (#201)
- ods2: derive SECURITY.SYS VBN1 checksum from lab-2, resolve BADCHECKSUM (increment 4, vms-0f3) (#200)

## Fixes

- fix: clear vms-0b8 quarantine — host-tool PT_INTERP poisoning + missing imgact include + /vms-needs-root harness (3 CI jobs) (#238)
- fix: per-facility negctl shards 3&4 green — re-stage test_imgact_* + fatal-path uses init.sh run order (vms-e7d) (#233)
- fix: main-red — reclassify test_syssvc_imgact_bind as userspace (closes #226) (#228)
- fix: DECC$SHR whole-archive drops a glibc _FORTIFY_SOURCE leak (__fprintf_chk), vms-0b8 (#227)
- fix: pin the VMS file-protection encoding — sys$chkpro agreed with the wrong bits (vms-f81) (#214)
- fix: remove vmslnm-manager LNM$SYSTEM host fallback (vms-48ab, INV-6) (#202)

## Reverse-Engineering / Protocol

- vmsscs: RE — the rejoin coordinator returns cm_responses=0 because it is SEND-CREDIT-STARVED, not because op 0x02 is bad; OVMX never returns Pending Receive Credit (never initiates the op-6 special-credit request the oracle sends before op 0x02), so the coordinator cannot transmit its op 0x04 reciprocation (Credit Wait) — lead 1 (stray ack) REFUTED, lead 2 refined to the op-6/op-8 special-credit exchange (spec §4(O.17), vms-96f) (#290)
- vmsscs: RE — the rejoin readmission stalls on COORDINATOR-VC BLOAT, not the freeze frame's content; OVMX front-loads its own dir+MSCP discovery so op 0x02 lands at ss=21 behind the member's frozen recv_seq, where the oracle stays lean at ss=14 (spec §4(O.15), vms-e15) (#287)
- scs: RE — SCS send_seq is per-VC not per-connection; refute the vms-bc2 rejoin lead candidate, relocate the residual to a member recv_seq freeze (spec §4(O.14), vms-bc2) (#270)
- vmsscs: rejoin readmission — hold the stray cat-0x04 ack so op 0x02 rides the member connection as contiguous SYSAP msg#3 (vms-71d, spec §4(O.13)) (#267)
- dlm: kernel resource-directory + LOCAL mastering scaffolding (vms-ci.5 DB) (#264)
- feat(vmsscs): RECNXINTERVAL VC-breakage reconnect + CSB connectivity states (vms-c7d) (#263)
- cluster-spec: byte-ground the DLM SYSAP body from single-op lock captures (vms-ac4) (#262)
- cluster: CM computes CEVOTES/QUORUM and gates on quorum; consume+advertise VOTES (vms-7a9) (#257)
- vmsscs: rejoin drives CM readmission on the MEMBER-INITIATED VMS$VAXcluster connection, not OVMX's own outbound VC (vms-4838) (#254)
- vmsscs: RE the rejoin readmission — it runs on the MEMBER-INITIATED VMS$VAXcluster connection, and the CONNECT_RSP-builder candidate is refuted (vms-2248) (#252)
- vmsscs: op 0x02 REJOIN body — emit body[36:40] membership-generation ordinal (wire fidelity), and RULE OUT the op 0x02 body as the rejoin readmission blocker (vms-e15) (#243)
- vmsscs: LOCATE the rejoin stall at CM readmission (NOT the VC-START identity conflict) — clean HEAD bracket, two timing modes, spec §4(O.9) (vms-694) (#241)
- vmsscs: REFUTE the SHOW CLUSTER NEW display-gap — NEW tracks transition completion, not a missing display step (vms-071, §4(z)) (#240)
- vmsscs: the joiner-VC ACCEPT_RSP CONNNOACT was a FALSE alarm — OVMX builds+sends that frame (op=3 CONNECT-CONFIRM); FSM step was blind to it (vms-694, §4(O.8)) (#239)
- vmsscs: OVMX now MODELS its own VMS$VAXcluster membership — joiner=CLOSED was a teardown-sampling artifact (vms-694, §4(O.7)) (#237)
- vmsscs: REFUTE the VMS$VAXcluster con_sent frontier — the SYSAP connection reaches 0002 open; con_sent was a teardown-sampling artifact (vms-694) (#235)
- vmsscs: bracket the rejoin MSCP-silence fix — the accept is already driven; kill-switch shows it is not the admission discriminator (vms-694) (#232)
- vmsscs: the VC-START stall is a sec 4(w) identity conflict, not a regression; name it (vms-694) (#230)
- vmsscs: diagnose the JS_MSCP_CONNECT rejoin "silence" as the Rule of Total Connectivity (vms-694) (#229)
- cluster: per-peer Con.ID allocation — fix the NEW->MEMBER established-multi-peer join stall (vms-694) (#221)
- cluster: ground the NEW->MEMBER established-multi-peer stall in a Con.ID collision (vms-694) (#219)
- vmsscs: ground the MSCP$DISK CONNECT-REQUEST retransmit cadence at ~10s (vms-694) (#217)
- vmsscs: drive the join-sequencer's retransmit off the loop's own clock (vms-694) (#215)
- vmsscs: retry CONNECT_REQ through the peer's rejoin REJECT_REQ storm (vms-694) (#211)
- feat: MSCP disk server attaches a real ODS-2 unit and serves real blocks (vms-600) (#209)
