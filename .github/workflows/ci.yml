name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # -----------------------------------------------------------------------
  # Job 1: Build and unit test (host, shared libs, readline)
  # -----------------------------------------------------------------------
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: apt-${{ runner.os }}-cmake-gcc-readline
          restore-keys: apt-${{ runner.os }}-

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            cmake gcc make libc6-dev libreadline-dev

      - name: Cache CMake build
        uses: actions/cache@v4
        with:
          path: build
          key: cmake-build-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'src/**/CMakeLists.txt', 'src/**/*.c', 'src/**/*.h', 'src/**/*.S') }}
          restore-keys: cmake-build-${{ runner.os }}-

      - name: Configure
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS=ON \
            -DBUILD_TOOLS=ON

      - name: Build
        run: cmake --build build -j$(nproc)

      - name: Test
        run: |
          sudo mkdir -p /vms/SYS0/SYSCOMMON/{SYSEXE,SYSLIB,SYSMGR,SYSHLP}
          sudo chmod -R 777 /vms
          cd build && ctest --output-on-failure

  # -----------------------------------------------------------------------
  # Job 2: Docker build + smoke test
  # -----------------------------------------------------------------------
  docker-build:
    name: Docker Build & Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: ovmx-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test — vmsdcl SHOW TIME
        run: |
          docker run --rm \
            --entrypoint vmsdcl \
            ovmx-test:latest \
            -c "SHOW TIME"

      - name: Smoke test — Docker skips install (pre-installed state)
        timeout-minutes: 2
        run: |
          # The default entrypoint boots and then serves SSH forever — it
          # never exits, and its PID 1 does not die on SIGTERM. So we must
          # NOT wrap `docker run` in `timeout` (timeout without -k waits
          # forever for the child to exit, hanging the job to the 6h
          # ceiling — see vms-021). Instead run detached, read the boot
          # output from the logs, and force-remove the container, which
          # guarantees teardown regardless of signal handling.
          CID=$(docker run -d ovmx-test:latest)
          sleep 10
          OUTPUT=$(docker logs "$CID" 2>&1 | head -20)
          docker rm -f "$CID" >/dev/null 2>&1 || true
          echo "$OUTPUT"
          echo "$OUTPUT" | grep -qF '%STARTUP-I-SYSBOOT' \
            && echo "PASS: Docker detects pre-installed system" \
            || { echo "FAIL: Expected SYSBOOT skip-install message"; exit 1; }

  # -----------------------------------------------------------------------
  # Job 3: Persistent boot smoke test (QEMU)
  #
  # Builds the bootable image, boots QEMU twice with the same disk:
  #   Boot 1: blank disk → STARTUP.EXE initializes, formats, installs
  #   Boot 2: same disk  → STARTUP.EXE detects installed system, skips install
  # Proves install-once / boot-many persistence across reboots.
  # -----------------------------------------------------------------------
  persistent-boot:
    name: Persistent Boot Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build bootable image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: distro/Dockerfile.bootable
          push: false
          load: true
          tags: ovmx-boot:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run persistent boot smoke test
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/tests/qemu/test_persistent_boot.sh:/test.sh:ro \
            --entrypoint bash \
            ovmx-boot:latest \
            /test.sh

  # -----------------------------------------------------------------------
  # Job 4: Conformance tests
  #
  # Runs inside the builder stage (has gcc, source tree, and built libs).
  # The conformance programs are compiled and executed against the VMS libs.
  # Uses the GHA Docker layer cache populated by the docker-build job.
  # -----------------------------------------------------------------------
  conformance:
    name: Conformance Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: docker-build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build builder stage (uses GHA layer cache)
        uses: docker/build-push-action@v6
        with:
          context: .
          target: builder
          push: false
          load: true
          tags: ovmx-builder:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run conformance tests
        run: |
          docker run --rm \
            --entrypoint bash \
            -v "${{ github.workspace }}/tests/conformance/vms_programs:/tests/vms_programs:ro" \
            ovmx-builder:latest \
            /src/tests/conformance/run_conformance.sh

  # -----------------------------------------------------------------------
  # Job 5: Corpus conformance — compile tier1-examples against OVMX headers
  #
  # This is a metric job, not a gate. It NEVER fails the build due to
  # compile/link/run failures in the corpus — those are expected gaps.
  # It DOES fail on regressions: a program that previously passed but
  # now fails compilation.
  #
  # The JSON report is uploaded as a build artifact for trend analysis.
  # -----------------------------------------------------------------------
  corpus-conformance:
    name: Corpus Conformance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: docker-build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build builder stage (uses GHA layer cache)
        uses: docker/build-push-action@v6
        with:
          context: .
          target: builder
          push: false
          load: true
          tags: ovmx-builder:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run corpus conformance harness
        run: |
          docker run --rm \
            --entrypoint bash \
            -v "${{ github.workspace }}/tests/conformance/corpus_baseline.json:/src/tests/conformance/corpus_baseline.json:ro" \
            ovmx-builder:latest \
            -c "bash /src/tests/conformance/run_corpus.sh /src/tests/conformance/corpus_baseline.json" \
            > corpus_report.json
        # Exit code 0 = no regressions (even if many compile-fail)
        # Exit code 1 = regression detected (previously-passing program now fails)

      - name: Upload corpus report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: corpus-conformance-report-${{ github.sha }}
          path: corpus_report.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # Job 6: Static analysis (clang-tidy + cppcheck)
  #
  # Runs in parallel with all other jobs — no `needs:` dependency.
  # INFORMATIONAL ONLY: always exits 0, findings are uploaded as artifacts.
  # src/libvmssys/ is excluded (freestanding, no glibc — false positives).
  # src/kernel/    is excluded (standalone Makefiles, not in compile_commands.json).
  # -----------------------------------------------------------------------
  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: apt-${{ runner.os }}-static-analysis
          restore-keys: apt-${{ runner.os }}-

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            cmake gcc make libc6-dev libreadline-dev \
            clang-tidy cppcheck

      - name: Configure (generate compile_commands.json)
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS=ON \
            -DBUILD_TOOLS=ON \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build (required for generated headers)
        run: cmake --build build -j$(nproc)

      - name: Run clang-tidy
        run: |
          # Collect source files from compile_commands.json,
          # excluding freestanding (libvmssys) and kernel directories.
          python3 - <<'PYEOF'
          import json, subprocess, sys, os

          db_path = "build/compile_commands.json"
          if not os.path.exists(db_path):
              print("compile_commands.json not found, skipping clang-tidy")
              sys.exit(0)

          with open(db_path) as f:
              db = json.load(f)

          cwd = os.getcwd()
          excluded = ("src/libvmssys/", "src/kernel/")
          files = []
          for entry in db:
              rel = entry["file"].replace(cwd + "/", "")
              if not any(rel.startswith(ex) for ex in excluded):
                  files.append(entry["file"])

          if not files:
              print("No files to analyze after exclusions.")
              sys.exit(0)

          print(f"Running clang-tidy on {len(files)} files...")
          result = subprocess.run(
              ["clang-tidy", "-p", "build"] + files,
              capture_output=True, text=True
          )
          output = result.stdout + result.stderr
          print(output)
          with open("clang-tidy-results.txt", "w") as out:
              out.write(output)
          # Always exit 0 — findings are informational only
          sys.exit(0)
          PYEOF

      - name: Run cppcheck
        run: |
          cppcheck \
            --suppressions-list=cppcheck.suppressions \
            --enable=warning,performance,portability \
            --error-exitcode=0 \
            --std=c11 \
            -D_GNU_SOURCE \
            -I src/libvms/include \
            -I src/vmsrms/include \
            -I src/vmsfs/include \
            -I src/vmslnm/include \
            -I src/vmsprocess/include \
            -I src/vmsdcl/include \
            --suppress=missingInclude:src/libvmssys/* \
            --suppress=missingInclude:src/kernel/* \
            -j$(nproc) \
            src/ 2>&1 | tee cppcheck-results.txt || true
          echo "cppcheck complete (exit forced to 0)"

      - name: Upload clang-tidy results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: clang-tidy-results
          path: clang-tidy-results.txt
          if-no-files-found: ignore

      - name: Upload cppcheck results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cppcheck-results
          path: cppcheck-results.txt
          if-no-files-found: ignore

  # -----------------------------------------------------------------------
  # Job 7: User Acceptance Test — scripted VMS session over SSH
  #
  # Builds the Docker image (using GHA layer cache from docker-build),
  # starts the OVMX container with SSH, runs a scripted VMS admin session,
  # and validates output for VMS correctness and absence of Unix leaks.
  # -----------------------------------------------------------------------
  uat-session:
    name: VMS User Acceptance Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: docker-build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (uses GHA layer cache)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: ovmx-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install sshpass
        run: sudo apt-get update -qq && sudo apt-get install -y sshpass

      - name: Start OVMX container
        run: |
          docker run -d --name ovmx-uat -p 2222:22 ovmx-test:latest
          sleep 5

      - name: Run UAT session test
        run: |
          chmod +x tests/uat/vms_session_test.sh
          tests/uat/vms_session_test.sh
        timeout-minutes: 3

      - name: Dump container logs (diagnostics)
        if: always()
        run: |
          echo "=== ovmx-uat container logs (vmssshd/DCL stderr) ==="
          docker logs ovmx-uat 2>&1 | tail -100 || echo "(no logs)"

      - name: Cleanup
        if: always()
        run: docker rm -f ovmx-uat 2>/dev/null || true

  # -----------------------------------------------------------------------
  # Job 8: IMGACT.EXE activation proof (musl, aarch64)
  #
  # Gate on the OVMX image activator (bead vms-913.x). Runs the clean-room
  # proof harness inside a native-musl Alpine container: builds IMGACT.EXE,
  # activates a dynamic test executable through it (DT_HASH symbol lookup,
  # RELATIVE/GLOB_DAT/JUMP_SLOT/TLSDESC relocs, TLS), and asserts both the
  # success path (activated program prints PASS, exits 0) and the failure
  # path (missing shareable image -> %IMGACT-F-IMGNOTFND, nonzero exit).
  #
  # IMGACT.EXE is aarch64-only until bead vms-913.11 (x86_64). GitHub runners
  # are x86_64, so the harness runs under arm64 emulation via binfmt/QEMU.
  # This is a hard gate (rule 10): a broken activator fails the build.
  # -----------------------------------------------------------------------
  imgact-proof:
    name: IMGACT.EXE Activation Proof (musl)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Build + activate IMGACT.EXE in Alpine (musl, arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils make >/dev/null
              export CC=gcc
              sh /src/src/imgact/test/run_test.sh
            '

  # -----------------------------------------------------------------------
  # Job 9: OVMX_IMGACT build mode (musl, aarch64)
  #
  # Gate on the image-activation build mode (bead vms-913.3): configure +
  # build the whole tree with -DOVMX_IMGACT=ON and assert (via readelf) that
  # shareable images carry VMS SONAMEs + DT_HASH, executables carry
  # PT_INTERP=IMGACT.EXE + DT_HASH, STARTUP.EXE/IMGACT.EXE stay static, and
  # nothing uses GNU_HASH. aarch64-only until bead vms-913.11; runs under
  # arm64 emulation on the x86_64 runner.
  # -----------------------------------------------------------------------
  imgact-build-mode:
    name: OVMX_IMGACT Build Mode (musl)
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Configure + build + verify OVMX_IMGACT (arm64 musl)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache cmake gcc g++ musl-dev binutils make linux-headers >/dev/null
              export CC=gcc BUILD=/tmp/imgact-build
              sh /src/src/imgact/test/verify_imgact_cmake.sh
            '

  # -----------------------------------------------------------------------
  # Job 9b: OVMX_IMGACT build mode + activation (musl, x86_64)
  #
  # x86_64 counterpart of Job 9 (imgact-build-mode) + the IMGACT activation
  # proof, gating bead vms-be5. GitHub runners ARE x86_64, so this runs the
  # NATIVE x86_64 musl Alpine image with NO emulation (contrast the aarch64
  # jobs, which need setup-qemu-action). Two proofs:
  #   1. verify_imgact_cmake_x86_64.sh — configure+build the whole tree with
  #      -DOVMX_IMGACT=ON, assert PT_INTERP=IMGACT.EXE + DT_HASH layout, no
  #      GNU_HASH, and TLS uses the TLSDESC dialect (-mtls-dialect=gnu2) with
  #      no general-dynamic __tls_get_addr relocs the x86_64 IMGACT can't bind.
  #   2. run_test_x86_64.sh — build the x86_64 IMGACT.EXE backend and activate
  #      a gnu2-TLSDESC shareable + PT_INTERP consumer FOR REAL (QEMU=env, i.e.
  #      run natively since the runner is x86_64), then assert the missing-image
  #      failure path yields %IMGACT-F-IMGNOTFND.
  # -----------------------------------------------------------------------
  imgact-x86_64:
    name: OVMX_IMGACT Build + Activate (musl, x86_64)
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build + verify + activate OVMX_IMGACT (native x86_64 musl)
        run: |
          docker run --rm --platform linux/amd64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache cmake gcc g++ musl-dev binutils make linux-headers >/dev/null
              echo "=== 1/2: OVMX_IMGACT x86_64 build + layout + TLSDESC verify ==="
              CC=gcc READELF=readelf BUILD=/tmp/imgact-build-x86_64 \
                sh /src/src/imgact/test/verify_imgact_cmake_x86_64.sh
              echo "=== 2/2: x86_64 IMGACT.EXE activation proof (native, QEMU=env) ==="
              CC=gcc READELF=readelf QEMU=env \
                sh /src/src/imgact/test/run_test_x86_64.sh
            '

  # -----------------------------------------------------------------------
  # Job 10: LINK.EXE — VMS-native linker MVP (aarch64)
  #
  # Gate on the OVMX/VMS linker (bead vms-9dd): builds LINK.EXE + OVMXDUMP,
  # compiles a leaf object with gcc, links it into an OVMX shareable image
  # WITHOUT ld, and asserts the image is a valid ET_DYN whose .vms$sv symbol
  # vector exports the declared universal symbols + GSMATCH. aarch64-only for
  # now (LINK.EXE rejects non-aarch64 objects); runs under arm64 emulation.
  # -----------------------------------------------------------------------
  vmslink-mvp:
    name: LINK.EXE VMS-native Linker (MVP)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Build LINK.EXE + link an OVMX shareable image (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils >/dev/null
              export CC=gcc WORK=/tmp/vmslink-test
              sh /src/src/vmslink/test/run_test.sh
            '

  # -----------------------------------------------------------------------
  # Job 10b: DECC$SHR.EXE — the OVMX C run-time library shareable (aarch64)
  #
  # Gate on the C-RTL shareable (bead vms-61f.1, pillar vms-ade): LINK.EXE links
  # the WHOLE musl libc.a + libgcc.a (soft-float/long-double builtins) into a
  # single OVMX shareable carrying a .vms$sv symbol vector of the C-RTL universals
  # (malloc/free/memcpy/strlen/printf/...) — VMS-native, NO ld/ld.so. Asserts the
  # image links CLEAN (zero deferred externals) and exports the universals as
  # PROCEDURE. Runtime init is vms-61f.2. aarch64-only; runs under arm64 emulation.
  # -----------------------------------------------------------------------
  decc-shr:
    name: DECC$SHR VMS-native C-RTL Shareable
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Build LINK.EXE + link DECC$SHR from whole musl libc.a + libgcc.a (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils >/dev/null
              export CC=gcc WORK=/tmp/decc-shr-test
              sh /src/src/vmslink/test/run_decc_shr.sh
            '

  # -----------------------------------------------------------------------
  # Job 11: real IMGACT.EXE symbol-vector activation (aarch64)
  #
  # Gate on the VMS-native activation path (bead vms-714): builds IMGACT.EXE
  # with .vms$imp support, links a producer + consumer with LINK.EXE, installs
  # IMGACT at its PT_INTERP path, and runs the consumer FOR REAL (kernel ->
  # PT_INTERP=IMGACT.EXE -> resolve symbol vector + GSMATCH -> transfer). The
  # cross-image call must return 42; a GSMATCH-older producer must exit 44
  # (%IMGACT-F-GSMATCH), not crash. Runs under arm64 emulation.
  # -----------------------------------------------------------------------
  imgact-symvec:
    name: IMGACT.EXE Symbol-Vector Activation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Activate a VMS-native image through the real IMGACT.EXE (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils make >/dev/null
              export CC=gcc WORK=/tmp/symvec-act
              sh /src/src/imgact/test/run_symvec_activation.sh
            '

  # Job 12: real src/libvmssys linked + activated VMS-native (aarch64)
  #
  # The pillar milestone (vms-ade / bead vms-80a): the whole toolchain end to
  # end on REAL code. Compiles the actual src/libvmssys, links it into an OVMX
  # shareable image with LINK.EXE (no ld), and runs a consumer that calls the
  # real vms_strlen through the symbol vector — kernel -> PT_INTERP=IMGACT.EXE
  # -> activate (GOT/.vms$rel bias + TLS setup) -> transfer. Must return 5.
  # Built with -mno-outline-atomics so gcc inlines atomics (no libgcc helpers).
  # Runs under arm64 emulation.
  # -----------------------------------------------------------------------
  libvmssys-native:
    name: libvmssys VMS-native Link + Activate
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Link + activate real libvmssys with LINK.EXE + IMGACT.EXE (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils make >/dev/null
              export CC=gcc WORK=/tmp/libvmssys-native
              sh /src/src/imgact/test/run_libvmssys_native.sh
            '

  # -----------------------------------------------------------------------
  # Job 13: DECC$SHR C-RTL activation — musl runtime init at activation (aarch64)
  #
  # The C-RTL milestone (pillar vms-ade / bead vms-61f.2): a consumer actually
  # CALLS musl libc through DECC$SHR, VMS-native. LINK.EXE whole-archives musl
  # libc.a + libgcc.a into DECC$SHR.EXE (exporting malloc/snprintf/strtod plus
  # musl's own __init_libc bootstrap), links a consumer importing them, and runs
  # it FOR REAL: kernel -> PT_INTERP=IMGACT.EXE -> resolve the symbol vector,
  # then drive musl __init_libc (programs the thread pointer, builds the TCB/TLS
  # musl-style, sets the stack guard, makes malloc usable) -> transfer control.
  # The consumer computes snprintf("%d+%d")=5 + strtod("3.5")*2=7 + buf[0]=2 == 14.
  # Proves musl libc is live post-activation with NO ld/ld.so. Runs under arm64
  # emulation.
  # -----------------------------------------------------------------------
  decc-shr-activate:
    name: DECC$SHR C-RTL Activation (musl init)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Activate DECC$SHR + drive musl init through IMGACT.EXE (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils make >/dev/null
              export CC=gcc WORK=/tmp/decc-shr-act
              sh /src/src/imgact/test/run_decc_shr_activation.sh
            '

  # -----------------------------------------------------------------------
  # Job 14: lib-shareable transitive import activation (aarch64)
  #
  # The spine increment (pillar vms-ade / bead vms-e65) that unblocks the b65
  # lib-migration chain: LINK.EXE's emit_shareable can now BIND a LIBRARY
  # shareable's own libc/pthread CALL imports to DECC$SHR (PLT + import GOT +
  # .vms$imp), and IMGACT.EXE resolves that binding TRANSITIVELY at activation.
  # mk_decc_shr.sh builds DECC$SHR with the appended pthread universals; a lib
  # shareable TESTLIB$SHR imports pthread_mutex_lock/unlock + malloc + memset +
  # free from it and exports lib_compute; a consumer that names ONLY TESTLIB$SHR
  # runs FOR REAL and returns 42 — proving the lib's own imports bound transitively
  # to DECC$SHR (a real pthread/malloc path executed through it). Runs under arm64
  # emulation.
  # -----------------------------------------------------------------------
  shareable-import-activate:
    name: Lib-Shareable Transitive Import Activation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register QEMU binfmt handlers (arm64 emulation)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Bind a lib shareable's imports to DECC$SHR through IMGACT.EXE (arm64)
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}":/src -w /src \
            docker.io/library/alpine:3.20 sh -c '
              set -e
              apk add --no-cache gcc musl-dev binutils make >/dev/null
              export CC=gcc WORK=/tmp/shr-import-act
              sh /src/src/imgact/test/run_shareable_import_activation.sh
            '
