# Source of truth for the MMS/MMK row of the Compatibility Surface Register.
facility: mms-mmk
name: "MMS / MMK (Make)"
domain: toolchain
vms_ref: "OpenVMS MMS Reference Manual"
scope_1_0: in
plan_ref: vms-59a
summary: >
  RTL prerequisites (CLI$, LIB$TPARSE, SYS$FILESCAN, SYS$SETDDIR,
  LIB$GET_FOREIGN) are all complete on origin/main (P1-P5). MMK.EXE itself
  is not built: no mk_mmk.sh, no native-link vector entries, and the
  parse_tables.mar C-port (vms-486) is not built. Self-host gate — the 1.0
  self-host build uses ported MMS/MMK, not bespoke BUILD.COM.
last_reviewed: 2026-08-13
items:
  - {id: mms-mmk$rtl-prereqs, kind: feature, status: implemented, authenticity: real, vms: "RTL prerequisites for MMS/MMK (CLI$, LIB$TPARSE, SYS$FILESCAN, SYS$SETDDIR, LIB$GET_FOREIGN)", evidence: "src/libvms/rtl", notes: "P1-P5 complete on origin/main."}
  - {id: mms-mmk$mmk-exe, kind: utility, status: designed, authenticity: n/a, vms: "MMK.EXE — make-compatible build utility", evidence: "docs/design-self-host-mmk-spine.md", scope_1_0: in, notes: "MMK.EXE not built; no mk_mmk.sh, no native-link vector entries."}
  - {id: mms-mmk$parse-tables-port, kind: feature, status: absent, authenticity: n/a, vms: "parse_tables.mar C-port (grammar tables for MMS/MMK parser)", evidence: "tests/corpus/tier3-mmk/parse_tables.mar", scope_1_0: in, notes: "Corpus source present as reference; C-port not built (vms-486)."}
