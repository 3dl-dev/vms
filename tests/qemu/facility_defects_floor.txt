# tests/qemu/facility_defects_floor.txt
#
# DERIVED FLOOR (vms-d894) on the number of entries in facility_defects.sh's
# DEFECTS list. Read by cmd_coverage's section 6 -- see that function and the
# file header's "vms-d894" paragraph for what this does and does not prove.
#
# This is a SEPARATE artifact on purpose. facility_defects.sh's own anchor
# check (section 4, vms-279) already requires a defect deletion to touch two
# files (facility_defects.sh + a suite source); this floor adds a THIRD, so
# a shrink below the number below is no longer confined to the deleter's own
# edit -- it has to also lower this file, visibly, in a diff with no other
# content.
#
# NOT TAMPER-PROOF: lowering the number below to match a deletion still
# passes the check. Lower it ONLY for a tracked, intentional removal of a
# defect, in the same change that removes it and explains why -- never to
# make an unrelated deletion pass quietly.
#
# THIS IS NO LONGER THE ONLY COUNT FLOOR, and it is the weaker one (vms-d894
# round 2). Section 6b of cmd_coverage derives a second floor from
# tests/qemu/facility_negctl_observed.tsv -- the number of defects a PAST RUN
# OF THE DRIVER ACTUALLY EXECUTED, which no edit to this file can raise or
# lower. Both floors are applied and neither replaces the other, so a record
# covering fewer defects can never lower the number below.
#
# To raise the floor after legitimately ADDING defects, rerun and paste the
# result below the comments:
#   awk '/^DEFECTS="/{f=1;next} f&&/^"/{exit} f' tests/qemu/facility_defects.sh | wc -l
#
# Value as of the commit that added this file (vms-d894): 42 entries.
# Raised to 43 by vms-2b2's register-adopt-pid-not-reported (see its own
# entry in facility_defects.sh for what it covers and why).
# Raised to 46 by vms-053's lock-enq-immediate-grant-status-wrong,
# lock-deq-status-wrong and lock-convert-mode-not-updated (the first
# mutations anchored inside vms_ioctl_enq/deq/convert's own bodies -- see
# those entries in facility_defects.sh for what each covers and why).
# Raised to 48 by vms-68e's proctab-getjpi-nonexpr-status-wrong and
# proctab-procscan-nonexpr-status-wrong (the first mutations anchored
# inside vms_ioctl_getjpi/procscan's own bodies -- see those entries in
# facility_defects.sh for what each covers and why).
# Raised to 51 by vms-2e7's devtab-dassgn-status-wrong,
# devtab-getdvi-devnam-status-wrong and devtab-devscan-found-status-wrong
# (the first mutations anchored inside vms_ioctl_dassgn/getdvi/devscan's
# own bodies -- see those entries in facility_defects.sh for what each
# covers and why).
# Raised to 55 by vms-177's eflag-setef-status-inverted,
# eflag-readef-status-inverted, eflag-dacefc-status-wrong and
# eflag-dlcefc-status-wrong (the first mutations anchored inside
# vms_ioctl_setef/readef/dacefc/dlcefc's own bodies -- see those entries in
# facility_defects.sh for what each covers and why). A fifth,
# eflag-ascefc-reassoc-status-wrong, was written and then DROPPED: real
# execution showed vms_ioctl_ascefc's own re-association status is only
# reachable in this tree through an unsynchronized race between two
# processes in test_kmod_eflag_mproc.c (parent and child both call
# sys$ascefc on the same cluster name immediately after fork(), no
# ordering between them), so which of two possible assertions goes red is
# genuinely nondeterministic -- structurally incompatible with this
# manifest's exact-match model. Left for a future item that either adds
# synchronization to that test or finds a differently-scoped target.
# Raised to 57 by vms-2ed's eflag-wflor-status-wrong and
# eflag-wfland-status-wrong (the first mutations anchored inside
# vms_ioctl_wflor/wfland's own bodies -- see those entries in
# facility_defects.sh for what each covers and why).
# Raised to 58 by vms-400's eflag-ascefc-reassoc-status-wrong -- the fifth
# attempt named above at line 51, this time landing: test_kmod_eflag_mproc.c
# was fixed to de-race its parent/child $ASCEFC calls (a token handshake so
# the parent always creates the cluster and the child always re-associates)
# before this entry was written, so the assertion it names is no longer
# scheduler-dependent. See its own entry in facility_defects.sh for the
# full account.
# Raised to 60 by vms-221's rms-create-filespec-not-translated -- the first
# entry to target vmsrms/rms_core.c (a userspace consumer of vmsfs's
# translation, same class as the vmsdcl entries) instead of src/kernel or
# src/vmsdcl, anchoring the new test_syssvc_rms_scratch_create suite so it
# is not left with a permission-only claim of being reddenable. cmd_selftest
# also grew a copy of src/vmsrms into its throwaway tree for this entry (see
# facility_defects.sh's cmd_selftest, "libvms, vmsdcl and vmsrms are copied
# too"). (The 59th, following #176's addition to the manifest, was already
# on the branch this rebased onto.)
# Raised to 61 by vms-e5c's scratch-dir-owner-not-system -- anchors
# test_syssvc_scratch_writable, whose own duplicated directory-provisioning
# helper (not src/ovmx_init/ovmx_init.c, which this suite never links) is
# the mutation target; see its own entry in facility_defects.sh for what it
# covers and why.
# Raised to 62 by vms-d37's lnm-delete-noop -- the first mutation targeting
# kernel/vms_lnm.c (executive-resident LNM$SYSTEM), anchoring the new
# test_syssvc_lnm_crossproc suite's cross-process DEASSIGN/SYSTEM assertion.
# See its own entry in facility_defects.sh for what it covers and why.
# Raised to 63 by vms-4d7's register-continue-identity-dropped -- anchors the
# new test_syssvc_identcont suite (image activation continues its activator's
# VMS identity, Option B), the first mutation targeting
# vms_proc_continue_identity() in kernel/vms_module.c. See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 64 by vms-48ab's lnm-manager-delete-noop -- anchors the new
# test_syssvc_lnm_system suite (the vmslnm-MANAGER API's LNM$SYSTEM path,
# as distinct from the sys$ API test_syssvc_lnm_crossproc.c already covers),
# the first mutation targeting vmslnm/lnm_client.c. See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 65 by vms-aba's lnm-group-scope-collapsed -- anchors the new
# test_syssvc_lnm_groupjob suite (LNM$GROUP/LNM$JOB executive residency, the
# deferred other half of vms-d37's LNM$SYSTEM work), the first mutation
# targeting derive_scope_key()'s LNM$GROUP case in kernel/vms_lnm.c. See its
# own entry in facility_defects.sh for what it covers and why.
#
# NOTE: this file was not updated when lnm-privilege-check-bypassed and
# mbx-not-shared landed (DEFECTS reached 67 without a matching entry here).
# Not fixed retroactively by this edit -- the floor is a MINIMUM, and 67 >=
# 65 still passed -- but flagged so the gap is not mistaken for a re-derived
# count. The line below only asserts the count as of THIS entry, vms-5bdc.
#
# Raised to 68 by vms-5bdc's p0-map-not-recorded -- vms-68f.i, the P0
# program-region bookkeeping foundation of the Option A in-process image
# activation design (docs/design-in-process-activation.md Part II). The
# first mutation targeting the new kernel/vms_p0.c, anchoring the new
# test_kmod_p0 suite (VMS_IOCTL_P0_MAP/P0_UNMAP, reflected through
# $GETJPI's new p0_base/p0_limit fields). See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 70 by vms-6ba's p1-map-not-recorded and p0-unmap-clears-p1 --
# vms-68f.ii, P1 control-region persistence and the P0/P1 distinction
# (same design). p1-map-not-recorded is the first mutation targeting the
# new kernel/vms_p1.c (VMS_IOCTL_P1_MAP, mirroring p0-map-not-recorded's
# shape on the new file); p0-unmap-clears-p1 targets vms_ioctl_p0_unmap()'s
# own body (kernel/vms_p0.c) and anchors the new test_kmod_p1 suite's key
# property, "P0 deleted on rundown, P1 survives", reflected through
# $GETJPI's new p1_base/p1_limit fields. See their own entries in
# facility_defects.sh for what each covers and why.
# Raised to 71 by vms-6f1's imgact-p1-not-protected -- vms-68f.iv, in-process
# image activation (imgact_activate(), the fork-per-image replacement). The
# first mutation targeting libvms/syssvc/sys_imgact.c, anchoring the new
# test_syssvc_imgact_inproc suite's enforced-half property, "a User-mode
# in-process image cannot corrupt DCL's critical P1" (the p1_protect the
# activation applies before ENTER_IMAGE). See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 74 by vms-cd48's image-rundown-leaks-user-lock -- vms-68f.v,
# SYS$RUNDWN image-scoped resource release (image rundown dequeues the locks,
# deassigns the channels and flushes the ASTs an activated image held at USER
# mode, while process-permanent inner-mode state survives). Targets
# vms_ioctl_image_rundown()'s own body (kernel/vms_access.c) and anchors the
# new test_kmod_rundown suite's key property. (This also closes the standing
# gap the note above flags: super-mode-escalation and image-rundown-without-
# entry, vms-68f.iii, had pushed the true count to 73 without the floor being
# raised; 74 is the re-derived `echo $DEFECTS | wc -w`.) See its own entry in
# facility_defects.sh for what it covers and why.
# Raised to 75 by vms-db2's consumer-import-not-bound-to-resident -- the
# §A.8-remainder import-binding-to-resident-shareable sub-step: a RUN'd image's
# .vms$imp imports must bind to the ALREADY-RESIDENT LIBVMS$SHR/DECC$SHR the
# process holds, not a private copy (which would share nothing). Targets
# libvms/syssvc/imgact_prodreg.c (imgact_bind_imports_resident's GOT-cell store)
# and anchors the new test_syssvc_imgact_bind suite's genuine-sharing property,
# proven in isolation without /dev/vms. See its own entry in facility_defects.sh.
75
