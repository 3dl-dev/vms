# SPDX-License-Identifier: GPL-2.0
#
# Makefile for the OVMX VMS-compatibility drivers (vms-934, parent vms-19e).
# One subdir per module; each subdir carries its own Kbuild. See Kconfig.

obj-$(CONFIG_OVMX_VMS)   += vms/
obj-$(CONFIG_OVMX_VMSFS) += vmsfs/
