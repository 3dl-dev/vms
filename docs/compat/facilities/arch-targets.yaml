# Source of truth for the Architecture Targets row of the Compatibility Surface Register.
facility: arch-targets
name: "Architecture Targets (x86_64, aarch64, alpha, vax)"
domain: runtime-arch
vms_ref: "N/A — OVMX platform targets, not a single VMS surface"
scope_1_0: in
plan_ref: vms-8ce
summary: >
  x86_64 is verified (full CI, vms.ko via /dev/vms). aarch64 is verified as
  a boot proof (freestanding EM_AARCH64 PID 1 under qemu-system-aarch64 in
  CI). Alpha (AXP) was made first-class by operator ruling (2026-08-10) but
  is absent on origin/main — the freestanding syscall/crt0 layer exists
  only on the unmerged local branch vms-054-alpha-port, and LINK.EXE has no
  EM_ALPHA backend. VAX is absent as a native libvmssys/LINK.EXE target;
  it exists only via a separate NetBSD-substrate path (aspirational/stretch).
last_reviewed: 2026-08-13
items:
  - {id: arch-targets$x86_64, kind: feature, status: verified, authenticity: real, vms: "x86_64 platform target", evidence: ".github/workflows/ci.yml", verified_against: "CI job 'Build & Test' (.github/workflows/ci.yml:272) — vms.ko via /dev/vms, full test suite", notes: "Primary arch (Rule 5)."}
  - {id: arch-targets$aarch64, kind: feature, status: verified, authenticity: real, vms: "aarch64 platform target", evidence: ".github/workflows/ci.yml", verified_against: "CI job 'aarch64-system-boot' (.github/workflows/ci.yml:207) — freestanding EM_AARCH64 PID 1 boots under qemu-system-aarch64 -machine virt, negctl gate", notes: "Boot-proof verified, not full CI parity with x86_64."}
  - {id: arch-targets$alpha, kind: feature, status: absent, authenticity: n/a, vms: "Alpha (AXP) platform target", evidence: "src/libvmssys/arch", scope_1_0: in, notes: "Made first-class by operator ruling 2026-08-10 (vms-8ce), but absent on origin/main: src/libvmssys/arch/ has only aarch64/x86_64, LINK.EXE has no EM_ALPHA target (src/vmslink/link.c:1694). Freestanding syscall/crt0 exists only on the unmerged local branch vms-054-alpha-port."}
  - {id: arch-targets$vax, kind: feature, status: partial, authenticity: n/a, vms: "VAX platform target", evidence: "src/kernel-netbsd", scope_1_0: stretch, notes: "Absent as a native libvmssys/LINK.EXE target; present only as a separate NetBSD-substrate path (src/kernel-netbsd, netbsd_vax CI, tests/lab-vax). libvmssys VAX backend commit vms-9dc not merged. Aspirational, not a 1.0 blocker."}
