# Source of truth for the LINK.EXE row of the Compatibility Surface Register.
facility: link
name: "LINK.EXE (Image Linker)"
domain: toolchain
vms_ref: "VSI OpenVMS Linker Utility Manual"
scope_1_0: in
plan_ref: vms-ade
summary: >
  LINK.EXE is a real, substantial implementation (2197 lines, 11 test
  scripts): symbol resolution, relocation, symbol-vector binding, GSMATCH,
  ar whole-archive ingestion. Targets EM_X86_64 and EM_AARCH64 only — no
  Alpha (EM_ALPHA) or VAX backend exists on origin/main yet.
last_reviewed: 2026-08-13
items:
  - {id: link$core-linker, kind: utility, status: implemented, authenticity: real, vms: "LINK.EXE image linker — symbol resolution, relocation, image build", evidence: "src/vmslink/link.c", notes: "2197 lines; 11 test scripts. Semantics real; image-container byte layout Rule-8-labeled (see image-activation/symbol-vectors). Emits x86_64/aarch64 only — which host architectures OVMX targets is a platform/roadmap concern (release dimension), not a VMS surface."}
