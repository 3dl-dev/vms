# Source of truth for the accounting row of the Compatibility Surface Register.
facility: accounting
name: "Accounting"
domain: system-management
vms_ref: "OpenVMS Guide to System Security; DCL Dictionary (SET/SHOW ACCOUNTING)"
scope_1_0: in
plan_ref: vms-8ad
summary: >
  A thin, real slice: per-user last-login timestamps and a persisted,
  system-wide accounting-enabled flag that SET/SHOW ACCOUNTING and the
  record path both honor (vms-17d moved this from a per-DCL-context facade to
  a real, shared flag). No ACCOUNTING.DAT journal — login/logout, process,
  and image accounting records are not implemented.
last_reviewed: 2026-09-14
items:
  - {id: accounting$last_login,   kind: feature, status: implemented, authenticity: real, vms: "Per-user last-login timestamp tracking", evidence: "src/libvms/rtl/ovmx_accounting.c:113"}
  - {id: accounting$enabled_flag, kind: feature, status: implemented, authenticity: real, vms: "SET ACCOUNTING/SHOW ACCOUNTING enabled state — real, persisted, system-wide (gates the record path, not a per-context bool)", evidence: "src/libvms/rtl/ovmx_accounting.c:76", plan_ref: vms-17d, notes: "Re-census 2026-09-14: unchanged. ovmx_accounting_set_enabled()/is_enabled() persist a real system-wide flag; SET ACCOUNTING in dcl_cmd_set.c drives it and ovmx_accounting_record_login() honors it (ovmx_accounting.c:119 early-returns when disabled) — no longer the per-context INV-DCL facade it replaced."}
  - {id: accounting$journal,      kind: feature, status: absent,      authenticity: n/a,  vms: "ACCOUNTING.DAT journal — login/logout, process, image, print accounting records", evidence: "", notes: "No live accounting daemon or record journal; only last-login + enabled-flag exist"}
