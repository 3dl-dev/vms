surface: vax-show-cpu
arch: vax
description: SHOW CPU layout — multiprocessing state + PRIMARY/Active/Configured CPU labels (numbers masked)
oracle: OpenVMS VAX V7.3 (lab vaxlab/vax1, isolated replica vaxlab-2)
captured: 2026-08-30T05:16:28Z
method: capture_oracle.sh -- FIFO console drive, marker-sliced display (CLAUDE.md Rule 8 observation)
normalize: 's/[0-9]/#/g'   # SYMMETRIC mask -- the vms-c38 gate applies the IDENTICAL
                          # mask to OVMX output: capture_oracle.sh normalize vax-show-cpu < ovmx_output
commands:
  - SHOW CPU
